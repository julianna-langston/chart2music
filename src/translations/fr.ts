import type { translationDict } from ".";

const dictionary: translationDict = {
    updated: "{title} mis à jour" // machine translated. not currently confirmed with a french speaker.
};

export default dictionary;
